=== Codex Plan Review Raw Output (R1) ===
Topic: 2026-04-24-super-admin-cross-school-account
Plan file: docs/plans/2026-04-24-super-admin-cross-school-account-plan.md
Design file: docs/plans/2026-04-24-super-admin-cross-school-account-design.md
Plan commit: 8f9c03540476e77f5c32282a0d777c86ab6c825f
Round: R1
Tier: T3
MCP threadId: 019dc035-3d62-72f3-8fb5-08eebddb4caf
Timestamp: 2026-04-24T23:45:00+08:00
Method: mcp__codex__codex (MCP deep path, after codex exec hung on stdin)

=== GPT 5.4 Independent Review Output ===

**Findings**
- ID: F001
- Severity: HIGH
- Category: 完整性 / 不变量与契约
- Type: test_gap
- Before-behavior: 当前 plan 把 ORC-004 的回放只落在前端下拉选项，Task 1 的 6 个后端测试没有覆盖"`platform_admin` 跨校创建 `subject_teacher` 仍然允许"这一后端契约。
- After-behavior: plan 应补一个 API 入口级测试，明确锁定"超管跨校传 `role=subject_teacher` 返回 201，且后端不收窄 `ALL_SCHOOL_ROLES`"。
- Evidence: `docs/plans/2026-04-24-super-admin-cross-school-account-design.md:152-157` 明确列出该场景；`docs/plans/2026-04-24-super-admin-cross-school-account-design.md:242` 把"后端不做角色白名单"写入 ORC-004；但 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:183-285` 的 6 个后端测试无此用例，且 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:959-963` 仅把 ORC-004 映射到前端 Step 2.1。
- Impact: 执行者即使误加"超管跨校只允许 `principal`/`academic_director`"的后端白名单，也可能全部过测，直接破坏 design 非目标。
- Repair hypothesis: 在 Task 1 增补 `platform_admin + school_id + roles=['subject_teacher'] -> 201` 的契约测试，并把 ORC-004 映射到该 Step。

- ID: F002
- Severity: HIGH
- Category: 完整性 / 测试契约质量
- Type: test_gap
- Before-behavior: Step 1.2 声称"6 个测试全红"，但按当前代码，至少 `test_platform_admin_creates_academic_director` 会直接变绿，红灯基线不成立。
- After-behavior: 红灯阶段应只包含在现状代码下一定失败的测试，或收紧该测试断言使其在当前实现下失败。
- Evidence: `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:208-222` 该测试只断言 201；现状 `src/edu_cloud/modules/student/teacher_router.py:153-188` 不会校验缺失 `school_id`，而是直接创建 `UserRole`；`src/edu_cloud/models/user_role.py:17-19` 显示 `school_id` 可空；本地验证 `.venv/bin/python` 下 Pydantic `BaseModel` 默认忽略额外字段，输出 `{'x': 1}`、`{}`，说明未声明的 `school_id` 不会触发"unknown field"错误，与 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:295` 的失败原因描述不符。
- Impact: TDD 的 Red→Green 纪律被破坏，执行者会在"并未全红"的前提下继续实现，Gate 1 的测试充分性判断失真。
- Repair hypothesis: 把该测试改为同时断言落库 `school_id`，或把 Step 1.2 的预期改成真实的失败分布并解释哪些用例先绿。

- ID: F003
- Severity: HIGH
- Category: 自洽性 / 行为契约
- Type: defect_fix
- Before-behavior: 当前页面 `defaultForm()` 默认 `roles = ['subject_teacher']`，`openCreate()` 会重置回该值；plan 只裁剪可选项，不重置已选值，且提交时原样发送 `payload.roles`。
- After-behavior: 超管跨校创建入口打开时，表单默认角色必须落在 `principal` / `academic_director` 允许集合内，且切校后不能残留 `subject_teacher`。
- Evidence: 现状 `frontend/src/pages/TeachersPage.vue:168-174`、`frontend/src/pages/TeachersPage.vue:288-291`；计划仅在 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:640-660` 改 `roleOptions`，在 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:711-727` 注入 `school_id`，没有任何一步清洗 `form.roles`；ORC-004 目标见 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:94`。
- Impact: 主流程里用户即使不主动改角色，也可能提交跨校 `subject_teacher`；而后端按 design 又故意不拦截，实际行为将偏离"仅创建学校管理账号"的目标。
- Repair hypothesis: 在 `openCreate()`/选校切换处重置或校验 `form.roles`，并补一个"超管跨校打开表单未手改角色时，payload 仍只能是两种管理角色"的测试。

- ID: F004
- Severity: MED
- Category: 代码库对齐 / 范围控制
- Type: defect_fix
- Before-behavior: plan 把全局 `roleOptions` 改成跨校计算属性，但该变量当前同时服务"创建表单"和"导入 Excel 默认角色"两个入口。
- After-behavior: 只有创建表单的角色下拉应被裁剪；导入入口若未在 spec 中声明变更，应保持原有全集角色。
- Evidence: `frontend/src/pages/TeachersPage.vue:77-103` 显示创建表单与导入弹窗都使用 `roleOptions`；`frontend/src/pages/TeachersPage.vue:140`、`frontend/src/pages/TeachersPage.vue:160` 为其默认值与定义；plan 在 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:640-660` 直接全局替换；design 只在 `docs/plans/2026-04-24-super-admin-cross-school-account-design.md:118-136` 讨论"创建表单角色下拉限制"。
- Impact: Excel 导入路径会被顺带改行为，超出本次目标，且可能让超管无法按原契约导入其他教师角色。
- Repair hypothesis: 将创建表单与导入默认角色拆成独立选项源，并给导入路径补回归测试。

- ID: F005
- Severity: MED
- Category: 完整性 / 测试契约质量
- Type: test_gap
- Before-behavior: Task 2 的测试主要通过 `wrapper.vm.selectedSchool`、`wrapper.vm.openCreate()`、`wrapper.vm.handleSave()` 与 `defineExpose` 读写内部状态，没有从用户可触达 DOM 入口验证 ORC-003 的"下拉是否渲染"。
- After-behavior: 至少应有一条入口级测试，从页面渲染结果验证学校下拉的存在/不存在，并通过交互触发保存，而不是仅调用内部方法。
- Evidence: `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:529-598` 全部断言都依赖 `wrapper.vm`；`docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:739-755` 还专门要求 `defineExpose`；而 ORC-003 在 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:92` 写的是 DOM 渲染条件。审查规范 `review-templates.md:275-280` 明确"无入口级验证"是 `test-gap MED`。
- Impact: 即使模板上的 `v-if`、绑定名或组件接线写错，只要内部计算属性还在，测试仍可能误绿。
- Repair hypothesis: 增加 DOM 级断言与用户交互驱动的提交测试，避免只测内部状态镜像。

- ID: F006
- Severity: MED
- Category: 自洽性 / 回滚与验证
- Type: process
- Before-behavior: Task 3 的手工验证既不可靠也无回滚：场景 C 读取 `localStorage.getItem('edu_cloud_token')`，而真实存储键是 `token`；场景 A/D 还会在 `mcu.asia` 创建真实校长账号，但 plan 没有删除或禁用的清理步骤。
- After-behavior: 手工 gate 应使用实际 token 键，并为创建的试验账号提供明确 cleanup/rollback。
- Evidence: 错误 token 用法见 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:867-877`；真实存储键见 `frontend/src/stores/auth.js:32`、`frontend/src/stores/auth.js:67`、`frontend/src/stores/auth.js:117`；真实创建账号见 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md:849-882`，之后直到 `...plan.md:903` 都无清理步骤。
- Impact: 手工验证可能得到 401 而不是预期 403；同时会向正在服务的环境残留测试账号，违反 checklist A 对破坏性操作的回滚要求。

- ID: F007
- Severity: MED
- Category: 完整性 / 审查流程
- Type: process
- Before-behavior: plan 虽有 5 字段测试契约，但每个 Task 都缺少 `审查清单` 与 `边界条件` 段。
- After-behavior: 每个行为变更 Task 都应补齐 `审查清单`，并列出至少 3 个边界条件及期望行为。
- Evidence: 对 `docs/plans/2026-04-24-super-admin-cross-school-account-plan.md` 执行 `rg -n "审查清单|边界条件"` 为零命中；而权威规范 `review-templates.md:206-215` 将这两段列为 `writing-plans` 必填要求。
- Impact: 执行者缺少明确边界规格，容易把关键异常路径留到 code review 才暴露，直接降低 Gate 1 通过率。

**结论**
FAIL

理由很明确：至少有 3 条 HIGH finding 直接阻塞本 plan 进入实现阶段——`ORC-004` 的后端不变量没有被测试回放、Task 1 的红灯基线不成立、以及前端跨校创建默认角色仍可能泄漏成 `subject_teacher`。另外，前端测试入口层级不足、Task 3 手工 gate 指令错误且无回滚、plan 格式也未满足权威模板要求。建议先修正这些问题，再进入实现。
