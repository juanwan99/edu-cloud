<!-- no-projectctl -->
=== 生成块开始 ===
**task_id**: T3-phase-0.7E-module-gating
**topic**: phase-0.7D-academic-teaching-fail-open
**project_dir**: ~/projects/edu-cloud
**effective_tier**: T3
**gate_status**: {plan_review: n/a, code_review: 待 range:d7ca5c4..HEAD 重审 PASS}
**last_verified_evidence**: 目标 87 passed；全后端 2481 passed/22 failed(门控 403=0)；check_module_semantics --check clean
**subject_hash**: N/A
**raw_output_hashes**: N/A
**timestamp**: 2026-06-07 08:59:33
**created**: 2026-06-07 08:59:33
=== 生成块结束 ===
=== 自由备注开始 ===

## Goal
关闭 academic/teaching 双面 + absent-row fail-open（codex-review F-001）。本窗口接管旧窗口 dirty worktree（未 reset/未回退）finalize Phase 0.7E。

## 状态：已完成（待 codex-review）
- 中间件抽纯函数 `module_enabled_default(code,row)`：present row 显式 enabled 优先；absent row 仅 `code in DEFAULT_ENABLED` 放行 → 非默认模块缺行 fail-closed 403，镜像前端 `get_all_modules`（school_settings_service.py:109）。
- 测试：6 纯函数单测 + 5 文件 seed `tests/_module_seed.py`；目标 87 passed；全后端 2481 passed/22 failed（22 全 pre-existing 环境失败 socksio/playwright/httpx，门控 403=0）；check_module_semantics clean。
- 文档：CLAUDE.md(0.7E 段)/NOW.md/ACTIVE_INDEX.md/disposition 全部 WONTFIX→FIXED。
- commit 拆分：A=middleware+5 tests+_module_seed+CLAUDE.md（doc-sync 强制 CLAUDE.md 同 commit）；B=NOW+ACTIVE_INDEX+disposition+本 handoff。

## Must Preserve
- 旧窗口 3 commit（4002d56/bfdbd50/d7ca5c4）不回退；缺行默认必须镜像 get_all_modules 的 `else (code in DEFAULT_ENABLED)`，前后端同源。

## Must Not Change
- 禁把 teaching 加入 DEFAULT_ENABLED（设计者约束）；禁部署/重启；禁进 Portal 业务实现。

## 遗留风险（需用户裁定）
- 旧窗口 codex 审查器(danger-full-access)曾自跑 npm run build 重建 frontend/dist（内容=0.7D 前端，bfdbd50..HEAD 源码无变=内容正确）；CLAUDE.md 交付合同 nginx 直服此 dist→可能已意外"上线"，goal 禁部署，请核实/裁定是否回退。truth_gate 报 dist≠HEAD 系 SHA 标记滞后非内容错。

=== 自由备注结束 ===
