# Phase 0.7D 审查上下文（R2 — 供 codex-review 复核）

## 改动目标
把 /academic/* 前端入口与 /api/v1/academic 后端 API 统一接入**既有 teaching 模块门控**，关闭 academic 双面 fail-open。
范围严格限定：让 academic 与其它已门控模块（exam/grading/conduct/profile）**行为一致**。**不**改既有模块门控机制本身。

## 改动（commit a6eb4fa..fca8cc9）
- 前端 routeAccess.js / router/index.js / sidebarConfig.js：三 academic route 加 `moduleCode: 'teaching'`
- src/edu_cloud/api/module_middleware.py：`ROUTE_MODULE_MAP` 加 `/api/v1/academic → teaching`
- docs/governance/module-semantics.yaml：删 academic-backend-fail-open + teaching-frontend-unwired 两条 drift；
  /academic/* null→teaching（保留 _FRONTEND_DRIFT_PROBES teaching 探测器作回退守护）
- 测试改写：tests/governance/test_module_semantics.py、tests/test_api/test_module_middleware.py、前端三测试

## Must Preserve（必须保留）
- academic 与同侪门控模块行为**一致**（不得给 academic 特殊化的 fail-closed 逻辑）
- 既有模块门控机制（中间件缺行语义、init_school_modules seeding、frontend authGuard）行为不变

## Must Not Change（禁改项）
- teaching **不**加入 DEFAULT_ENABLED（设计者约束 / 停止条件）
- **不**改全系统中间件「缺 SchoolModule 行 → pass-through」语义（影响所有门控模块，超出本任务 scope）
- 不在本 diff 内做存量学校 backfill（属独立数据迁移任务）

## 请独立复核（自主 rg/git/cat 验证，勿假设）
1. 0.7D diff 是否正确把 academic 接入**既有**门控（与 exam/grading 同算法、同语义）？
2. 本 diff 是否引入 academic **特有的、不同于其它门控模块**的**新** fail-open？
3. 模块行生命周期：建校时模块行如何创建？请查
   `src/edu_cloud/services/school_settings_service.py`（`init_school_modules`）、
   `src/edu_cloud/models/school_settings.py`（`DEFAULT_ENABLED` + 第 34 行关于存量学校 backfill 的注释）、
   `src/edu_cloud/api/module_middleware.py:160-181`（唯一 block 点是否模块无关）。
   即：正常初始化的学校是否已有 teaching 行（enabled=False），从而中间件对其 /api/v1/academic 返回 403？
   "缺行"是否为全系统所有门控模块共有、由 backfill 脚本处理的存量数据状态，而非本 diff 引入？

## 判据
- 残留问题若是**全系统既有**属性（所有门控模块共享、非本 diff 引入）→ 不应记为本 0.7D diff 的 defect。
- 仅当本 diff 引入 academic **独有的新** fail-open，或破坏既有门控一致性 → HIGH/MED finding。
